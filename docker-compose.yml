services:
  db:

  backend:
    image: yaponchick1337/simpleapp-backend:latest   # ✅ используем собранный образ
    container_name: simpleapp_backend
    ports:
      - "5215:8080"
    environment:
      - ConnectionStrings__DefaultConnection=Server=db;Database=SimpleApp;User Id=sa;Password=G3GDeR43RMfkf423Fff;TrustServerCertificate=True;
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - db
    restart: on-failure

  frontend:
    image: yaponchick1337/simpleapp-frontend:latest   # ✅ используем собранный образ
    container_name: simpleapp_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always